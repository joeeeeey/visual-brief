# Synthetic procedure source

All URLs in this fixture are training-only and do not point to a live portal.

1. The Device check page visibly provides a `Download helper` control.
2. The next page visibly names a `Training profile` and would be the safety stop before a real installation or permission change.
3. The final page visibly shows `Device check complete`.

The training flow does not authorize a real download, profile installation, or permission change.
